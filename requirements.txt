# spyder-screen-calibrator — Python dependencies
# Install with:  pip install -r requirements.txt

# Computer vision & image processing
opencv-python>=4.8.0

# Numerical computing
numpy>=1.24.0

# gPhoto2 Python bindings (persistent USB session)
# Requires: sudo apt install libgphoto2-dev
gphoto2>=2.3.0

# RAW file processing — supports CR3 (Canon M50 MK2) unlike dcraw
# Requires: sudo apt install libraw-dev
rawpy>=0.18.0
